#include <iostream>
using namespace std;

int calculate (float numBase = 0, float numInput = 0) {

    int I[9];
    if (numBase = numInput) {I[numBase].ignore; }
}
int main() {

    int num = 0;
    int numBase = 0;

    cout << "Please enter 9 numbers" << endl;
    int I[9];
    for (int num = 0; I[num] < I[num+1]; num++) {

        int numBase = calculate;
    }

    cout << I[num] << endl;
}